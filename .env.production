# Production build (pnpm build / pnpm run deploy → surge) talks to the live
# Render authority. Vite loads .env.production only in build mode, and it takes
# priority over .env.local, so the deployed client always uses the live server.
VITE_WS_URL=wss://hedgeways-server.onrender.com
